import { MemberWithDetailModel } from './member-with-detail-model';

describe('MemberWithDetailModel', () => {
  it('should create an instance', () => {
    expect(new MemberWithDetailModel()).toBeTruthy();
  });
});
