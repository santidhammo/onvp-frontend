import { AuthorizationRequest } from './authorization-request';

describe('LoginData', () => {
  it('should create an instance', () => {
    expect(new AuthorizationRequest()).toBeTruthy();
  });
});
