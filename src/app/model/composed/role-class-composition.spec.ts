import { RoleClassComposition } from './role-class-composition';

describe('RoleClassComposition', () => {
  it('should create an instance', () => {
    expect(new RoleClassComposition()).toBeTruthy();
  });
});
