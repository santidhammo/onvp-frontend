import { DetailRegisterSubCommand } from './detail-register-sub-command';

describe('DetailSubCommand', () => {
  it('should create an instance', () => {
    expect(new DetailRegisterSubCommand()).toBeTruthy();
  });
});
