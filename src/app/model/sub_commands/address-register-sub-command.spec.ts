import { AddressRegisterSubCommand } from './address-register-sub-command';

describe('AddressSubCommand', () => {
  it('should create an instance', () => {
    expect(new AddressRegisterSubCommand()).toBeTruthy();
  });
});
