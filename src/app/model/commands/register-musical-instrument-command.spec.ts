import { RegisterMusicalInstrumentCommand } from './register-musical-instrument-command';

describe('RegisterMusicalInstrumentCommand', () => {
  it('should create an instance', () => {
    expect(new RegisterMusicalInstrumentCommand()).toBeTruthy();
  });
});
