import { FirstOperatorRegisterCommand } from './first-operator-register-command';

describe('FirstOperator', () => {
  it('should create an instance', () => {
    expect(new FirstOperatorRegisterCommand()).toBeTruthy();
  });
});
