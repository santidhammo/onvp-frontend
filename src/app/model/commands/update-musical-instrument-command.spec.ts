import { UpdateMusicalInstrumentCommand } from './update-musical-instrument-command';

describe('UpdateMusicalInstrumentCommand', () => {
  it('should create an instance', () => {
    expect(new UpdateMusicalInstrumentCommand()).toBeTruthy();
  });
});
