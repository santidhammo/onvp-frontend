import { UpdatePageCommand } from './update-page-command';

describe('UpdatePageCommand', () => {
  it('should create an instance', () => {
    expect(new UpdatePageCommand()).toBeTruthy();
  });
});
