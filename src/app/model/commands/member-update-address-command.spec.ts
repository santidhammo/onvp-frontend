import { MemberUpdateAddressCommand } from './member-update-address-command';

describe('MemberUpdateAddressCommand', () => {
  it('should create an instance', () => {
    expect(new MemberUpdateAddressCommand()).toBeTruthy();
  });
});
