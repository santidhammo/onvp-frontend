import { CreateMailTemplateCommand } from './create-mail-template-command';

describe('CreateMailTemplateCommand', () => {
  it('should create an instance', () => {
    expect(new CreateMailTemplateCommand()).toBeTruthy();
  });
});
