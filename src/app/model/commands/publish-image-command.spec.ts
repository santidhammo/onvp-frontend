import { PublishImageCommand } from './publish-image-command';

describe('PublishImageCommand', () => {
  it('should create an instance', () => {
    expect(new PublishImageCommand()).toBeTruthy();
  });
});
