import { AssociateRoleCommand } from './associate-role-command';

describe('AssociateRoleCommand', () => {
  it('should create an instance', () => {
    expect(new AssociateRoleCommand()).toBeTruthy();
  });
});
