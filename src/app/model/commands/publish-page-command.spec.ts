import { PublishPageCommand } from './publish-page-command';

describe('PublishPageCommand', () => {
  it('should create an instance', () => {
    expect(new PublishPageCommand()).toBeTruthy();
  });
});
