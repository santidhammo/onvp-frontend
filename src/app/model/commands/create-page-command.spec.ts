import { CreatePageCommand } from './create-page-command';

describe('CreatePageCommand', () => {
  it('should create an instance', () => {
    expect(new CreatePageCommand()).toBeTruthy();
  });
});
