import { MemberActivationCommand } from './member-activation-command';

describe('TokenData', () => {
  it('should create an instance', () => {
    expect(new MemberActivationCommand()).toBeTruthy();
  });
});
