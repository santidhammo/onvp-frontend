import { DissociateRoleCommand } from './dissociate-role-command';

describe('DissociateRoleCommand', () => {
  it('should create an instance', () => {
    expect(new DissociateRoleCommand()).toBeTruthy();
  });
});
