import { UpdateMailTemplateCommand } from './update-mail-template-command';

describe('UpdateMailTemplateCommand', () => {
  it('should create an instance', () => {
    expect(new UpdateMailTemplateCommand()).toBeTruthy();
  });
});
