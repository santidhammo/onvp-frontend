import { MemberPictureAsset } from './member-picture-asset';

describe('MemberPictureAsset', () => {
  it('should create an instance', () => {
    expect(new MemberPictureAsset()).toBeTruthy();
  });
});
